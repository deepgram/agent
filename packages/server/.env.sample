# Required — API keys injected server-side by the proxy
DEEPGRAM_API_KEY=
ANTHROPIC_API_KEY=

# Optional — server port (default: 3001)
PORT=3001

# Optional — allowed CORS origins, comma-separated (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173
