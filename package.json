{
  "name": "@deepgram/voice-agent-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "packageManager": "bun@1.3.10",
  "scripts": {
    "build": "bun run --filter 'packages/*' build",
    "dev": "bun run --filter 'packages/*' dev",
    "dev:examples": "op run --env-file examples/.env -- bun run --cwd examples dev",
    "typecheck": "bun run --filter 'packages/*' typecheck",
    "lint": "bun run --filter 'packages/*' lint",
    "test": "bun run --filter '@deepgram/agent' --filter '@deepgram/agent-react' --filter '@deepgram/agent-widget' test",
    "test:e2e": "bun test tests/e2e/"
  },
  "overrides": {
    "brace-expansion": "5.0.5"
  }
}
