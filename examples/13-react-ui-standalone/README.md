# Standalone Card — React UI

Standalone agent card using all `@deepgram/ui` components with built-in styling.

**Package:** `@deepgram/ui`

![Screenshot](screenshot.png)

## Run

```bash
# From the repo root
bun run dev:examples
# Open http://localhost:5173/13-react-ui-standalone/
```
