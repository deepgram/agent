# Copy this file to .env and fill in your Deepgram API key.
# The key is NEVER sent to the browser — the dev server exchanges it
# for a short-lived access token at /api/token.
DEEPGRAM_API_KEY=your_deepgram_api_key_here
